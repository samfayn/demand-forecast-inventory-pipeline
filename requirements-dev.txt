# Development and test dependencies. Includes everything in requirements.txt.
-r requirements.txt

pytest>=7.0
pytest-cov>=4.0
pyflakes>=3.0

# Needed to run the notebooks in notebooks/, not by the app itself.
jupyter>=1.0